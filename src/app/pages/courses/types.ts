export interface Course {
    id: string;
    title: string;
    created: string;
    duration: number;
                    description: string;
}

export interface User {
    id: string;
    firstName: string;
    lastName: string;
}
